@model WebApplication1.Areas.Catalog.Entites.Category

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Category</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.IsPersistent)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.IsPersistent)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.IsActive)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.IsActive)
        </dd>

    </dl>

    @using (Ajax.BeginForm("Delete", "Category", new AjaxOptions() { UpdateTargetId = "articles" })) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color btn-group">
            <input type="submit" value="Delete" class="btn btn-default" />
            @Ajax.ActionLink("Back to List", "GetListInCategory", "Category", new { categoryId = Model.Id }, new AjaxOptions() { UpdateTargetId = "articles" }, new { @class = "btn btn-default" })
        </div>
    }
</div>
